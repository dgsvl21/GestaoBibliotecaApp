<UserControl x:Class="GestaoBibliotecaApp.View.LeitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GestaoBibliotecaApp.ViewModel"
             mc:Ignorable="d" 
             Height="500" Width="800">
    
    <UserControl.DataContext>
        <local:LeitorViewModel />
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" Margin="0.5">
                <Label Width="80" Content="Nome:" VerticalAlignment="Center"/>
                <TextBox Width="300" Margin="0,0,0,10" Height="30" Text="{Binding NovoLeitor.Nome, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0.5">
                <Label Width="80" Content="Email:" VerticalAlignment="Center"/>
                <TextBox Width="300" Height="30" Text="{Binding NovoLeitor.Email, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <Button Content="Adicionar Leitor"
                Width="150" Height="30" Margin="0,10,0,0"
                Command="{Binding AdicionarLeitorCommand}"/>

            <Button Content="Eliminar Leitor"
                Width="150" Height="30" Margin="0,10,0,0"
                Command="{Binding EliminarLeitorCommand}"/>

        </StackPanel>

        <DataGrid Grid.Row="1"
              ItemsSource="{Binding Leitores}"
              SelectedItem="{Binding LeitorSelecionado}"
              AutoGenerateColumns="False"
              ColumnWidth="*"
              IsReadOnly="True"
              Margin="0,10,0,0">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="23" Binding="{Binding LeitorId}"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" />

            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</UserControl>
